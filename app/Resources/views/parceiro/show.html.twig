{% extends 'base.html.twig' %}

{% block body %}
    <h1>Parceiro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parceiro.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ parceiro.nome }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ parceiro.descricao }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ parceiro.link }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="admin_botoes">
        <li>
            <a href="{{ path('admin_parceiro_index') }}" class="btn btn-default">Voltar</a>
        </li>
        <li>
            <a href="{{ path('admin_parceiro_edit', { 'id': parceiro.id }) }}" class="btn btn-default">Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Deletar" class="btn btn-danger">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
